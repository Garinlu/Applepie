import { SwipeRowTableDirective } from './hammer.directive';

describe('SwipeRowTableDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeRowTableDirective();
    expect(directive).toBeTruthy();
  });
});
