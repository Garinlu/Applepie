import { ShadowRowTableDirective } from './shadow-row-table.directive';

describe('ShadowRowTableDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowRowTableDirective();
    expect(directive).toBeTruthy();
  });
});
