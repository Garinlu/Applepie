import { RmColumnDirective } from './rm-column.directive';

describe('RmColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new RmColumnDirective();
    expect(directive).toBeTruthy();
  });
});
